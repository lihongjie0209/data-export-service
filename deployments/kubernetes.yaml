apiVersion: v1
kind: Namespace
metadata:
  name: platform
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: data-export-service
  namespace: platform
automountServiceAccountToken: false
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: data-export-service
  namespace: platform
data:
  APP_ENV: production
  APP_HTTP_ADDRESS: 0.0.0.0:8080
  APP_GRPC_ADDRESS: 0.0.0.0:9090
  APP_DATABASE_ENABLED: "true"
  APP_DATABASE_TYPE: postgres
  APP_DATABASE_NAME: platform
  APP_DATABASE_SCHEMA: data_export
  APP_REDIS_ENABLED: "true"
  APP_RATE_LIMIT_ENABLED: "true"
  APP_IDEMPOTENCY_ENABLED: "true"
  APP_EVENT_BUS_ENABLED: "true"
  APP_EVENT_BUS_URLS: nats://nats.platform.svc.cluster.local:4222
  APP_OBJECT_STORAGE_ENABLED: "true"
  APP_OBJECT_STORAGE_ENDPOINT: minio.platform.svc.cluster.local:9000
  APP_OBJECT_STORAGE_BUCKET: platform-exports
  APP_AUTH_AUDIENCE: data-export-service
  APP_SERVICE_REGISTRY_ENABLED: "true"
  APP_SERVICE_REGISTRY_TARGET: service-registry-service.platform.svc.cluster.local:9090
  APP_SERVICE_REGISTRY_TLS_ENABLED: "true"
  APP_SERVICE_REGISTRY_TLS_SERVER_NAME: service-registry-service.platform.svc.cluster.local
  APP_PROVIDER_CLIENT_TLS_ENABLED: "true"
  APP_OBSERVABILITY_METRICS_ENABLED: "true"
  APP_MIGRATION_AUTO_UP: "true"
  APP_MIGRATION_PATH: /app/migrations/postgres
  APP_MIGRATION_TABLE: data_export_schema_migrations
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: data-export-service
  namespace: platform
  labels:
    app.kubernetes.io/name: data-export-service
spec:
  replicas: 2
  revisionHistoryLimit: 5
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: data-export-service
  template:
    metadata:
      labels:
        app.kubernetes.io/name: data-export-service
    spec:
      serviceAccountName: data-export-service
      automountServiceAccountToken: false
      terminationGracePeriodSeconds: 30
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: kubernetes.io/hostname
          whenUnsatisfiable: ScheduleAnyway
          labelSelector:
            matchLabels:
              app.kubernetes.io/name: data-export-service
      containers:
        - name: api
          image: ghcr.io/lihongjie0209/data-export-service:main
          imagePullPolicy: IfNotPresent
          envFrom:
            - configMapRef:
                name: data-export-service
            - secretRef:
                name: data-export-service
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
            - name: grpc
              containerPort: 9090
              protocol: TCP
          startupProbe:
            httpGet:
              path: /live
              port: http
            periodSeconds: 2
            timeoutSeconds: 2
            failureThreshold: 30
          livenessProbe:
            httpGet:
              path: /live
              port: http
            periodSeconds: 10
            timeoutSeconds: 2
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /ready
              port: http
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3
          lifecycle:
            preStop:
              exec:
                command: [sh, -c, sleep 5]
          volumeMounts:
            - name: logs
              mountPath: /app/logs
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: "1"
              memory: 512Mi
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: [ALL]
            readOnlyRootFilesystem: true
      volumes:
        - name: logs
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: data-export-service
  namespace: platform
  labels:
    app.kubernetes.io/name: data-export-service
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: data-export-service
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
    - name: grpc
      port: 9090
      targetPort: grpc
      protocol: TCP
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: data-export-service
  namespace: platform
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: data-export-service
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: data-export-service
  namespace: platform
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: data-export-service
  minReplicas: 2
  maxReplicas: 10
  behavior:
    scaleDown:
      stabilizationWindowSeconds: 300
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: data-export-service
  namespace: platform
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: data-export-service
  policyTypes: [Ingress, Egress]
  ingress:
    - from:
        - podSelector: {}
      ports:
        - {port: 8080, protocol: TCP}
        - {port: 9090, protocol: TCP}
  egress:
    - to:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: kube-system
          podSelector:
            matchLabels:
              k8s-app: kube-dns
      ports:
        - {port: 53, protocol: UDP}
        - {port: 53, protocol: TCP}
    - ports:
        - {port: 3306, protocol: TCP}
        - {port: 5432, protocol: TCP}
        - {port: 6379, protocol: TCP}
        - {port: 4222, protocol: TCP}
        - {port: 9000, protocol: TCP}
        - {port: 9090, protocol: TCP}
        - {port: 4318, protocol: TCP}
        - {port: 443, protocol: TCP}
